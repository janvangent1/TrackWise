# TrackWise Web — Python dependencies
# Install with: pip install -r requirements.txt

# ── Web framework ──────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9     # for file upload (Form + UploadFile)

# ── GPX / geo ──────────────────────────────────
gpxpy>=1.6.2
geopy>=2.4.1
shapely>=2.0.6

# ── HTTP requests (Overpass, OSRM) ─────────────
requests>=2.32.0

# ── Optional: nicer startup logs ───────────────
# uvicorn uses colorlog when available
